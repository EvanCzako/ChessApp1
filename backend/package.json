{
  "name": "chess-server",
  "version": "1.0.0",
  "description": "Backend server to communicate with Stockfish engine",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build": "npm install --force && bash scripts/install-stockfish.sh"
  },
  "dependencies": {
    "chess.js": "^1.0.0-beta.8",
    "cors": "^2.8.5",
    "express": "^4.22.1"
  }
}
