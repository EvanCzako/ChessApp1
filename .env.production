# This file is used by Vite to determine which environment variables are available
# For Render deployment, ensure REACT_APP_API_URL is set in the environment

# These variables will be injected at build time
VITE_API_URL
